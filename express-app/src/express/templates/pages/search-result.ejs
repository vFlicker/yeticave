<%- contentFor('content') %>

<section class="promo">
  <h1>Search results</h1>
  <ul class="lots__list">
    <% lots.forEach((lot) => { %>
    <%- include("../partials/lot-card", { lot }) %>
    <% }) %>
  </ul>
</section>